<!--
=============================================================================
FILE BRIEF: The learning loop's inbox. Anyone — Singh, any AI session, a
failing check — files a row here when a rule looks wrong, incomplete, or does
not fit a case. Operated by the Design System Agents conversation; ruled on by
Singh only. A build that "violates" a rule which turns out to be RIGHT is a
rule defect, not a build defect — it belongs here, and the build is not "fixed".
CREATOR: Fable-WOUND-MGMT (per Singh's approved plan) | 2026-08-24
LAST MODIFIER: Fable-WOUND-MGMT | 2026-08-25 — closed F-003 as NOT-REPRODUCED (outcome
column only, row body untouched per this file's own append-only rule) and appended
F-012 + F-013. Edited at Opus's explicit request while Opus holds the Slot A lock;
Opus also asked the DESIGN-SYSTEM lane for the same F-003 correction, so if a
duplicate retraction row appears, this one (Outcome column of F-003) is the original.
=============================================================================
-->

# Rule Flags — open questions and challenges against the universal rules

**How to file:** append one row. Never edit or delete another row — outcomes are recorded in
the Outcome column by the maintainer, and closed rows stay for audit.

**Lifecycle:** OPEN → VALIDATED (reproduced against evidence) or NOT-REPRODUCED (closed,
reason logged) → Singh rules → RULE-UPDATED vX / EXCEPTION EX-nnn / REJECTED.

| Flag | Date | Rule / area | Filed by | What happened (with evidence path) | Status | Outcome |
|---|---|---|---|---|---|---|
| F-001 | 2026-08-24 | (seed) breakpoint set | Fable, from SPEC-07 H-11 | Four non-identical breakpoint definitions on record; uncontested: design widths 1200/800/300, tablet band 800–1199; contested: mobile ceiling 480 vs 799, desktop start 1200 vs 1440. Evidence: SPEC-07 §D.1. Every responsive build guesses until ruled. | OPEN — awaiting Singh | — |
| F-002 | 2026-08-24 | (seed) focus-ring token | Fable, from SPEC-07 H-15 | WCAG 2.4.7 obligation with no conforming token binding anywhere. Which token, what width/offset? | OPEN — awaiting Singh | — |
| F-003 | 2026-08-24 | (seed) coloured clinical-status tag missing from DS | Fable, from Wound Mgmt Q-A | `Status/info` (10313:14623) is the real status component but has 3 variants and no clinical hues; every hue'd tag set is scheduling-specific. Features need a generic coloured status tag. Evidence: `Wound Management/05-Specs/FIGMA-QUERY-LIST-BLOCKING-DESIGN.md` Q-A. | **CLOSED 2026-08-25 — NOT-REPRODUCED. The claim was FALSE: the component exists.** | **`Status` — node `26535:26277`, Design System Agency V2.0 (`4bh29laapcuKBTghfaRXF0`), found by Opus from a link Singh supplied.** Its own Figma description names it the catch-all: *"Active/inactive state badges. and all otehr statsus like Nutral, Succes, Sepcial, AI, Alerts, Error etc. basically here we will conver all kind of statsues."* Two axes, `type` (9 values) × `subtleBorder` (bool). **This flag looked for a hue axis on `badges/other` and on `Status/info` and concluded from their absence that no component had one — a partial read generalised into an absence claim, which is the single error type this register is least able to self-correct.** Superseded by the far narrower **F-012**. **Root cause worth more than the flag: the answer was already on disk.** `SPEC-08a` §148 marks the tag label ✅ **CONFIRMED** (Inter Semi Bold 10 UPPERCASE, `font-weight/heading-strong`, `font-size/micro`, style `Label/overline`), §291 tabulates it, and **§540–558 carries the complete `.c360-tag` CSS** — uppercase, semibold, micro, and **no dot**. I cited SPEC-08a as the authority for the chip's container while contradicting its own code block 90 lines below in the same file, because I judged the type "attested for `badges/other` ONLY". **Citing a spec for one property while declaring another property unattested — when that spec answers both — is the generalisable defect.** |
| F-004 | 2026-08-24 | (seed) density vocabulary | Fable, from folder sweep | Three vocabularies live at once: `compact/default/comfortable` (poc-design-system selectors), `small/medium/large` (caresmartz360-design-system comment over MISMATCHED selectors), `Default/Large/Small` (Caregiver export). Evidence: both repos' `styles.scss:19` + `Variables/Caregiver`. | OPEN — awaiting Singh/Figma AI | — |
| F-005 | 2026-08-24 | Caregiver `border/transparent` is OPAQUE in High Contrast | Fable, verified from the raw export | A token named "transparent" resolves to `alpha 0` in Light Mode + Soothing Dark but **`#001F52`, `alpha 1`** in High Contrast. Plausibly deliberate (HC needs visible borders where other modes have none) — but the name then misleads anyone binding it, and Caregiver's copy carries no `$description` while Agency's does ("Transparent/invisible border"). Evidence: `Variables/Caregiver/Color Theme.zip` → `High Contrast.tokens.json` → `border/transparent`. | OPEN — routed to Figma AI via Opus | — |
| F-006 | 2026-08-24 | Token pipelines must preserve alpha | Fable, from a defect in own generator | Every token pipeline that reads the DTCG export must emit `rgba()` when `alpha < 1`. A generator that returns only `hex` silently flattens 25 translucent tokens (incl. Caregiver's 40% modal scrim and all `action/*/border` "no border" values) and **no output-only check can detect it** — proven: 6 existing checks passed on the defective output. Proposed as a universal rule + the promotable check `verify-token-fidelity.py` (generated CSS compared back to the raw export). Fixed in the Wound Mgmt generator; ledger row 24. | OPEN — proposed for `01-universal/css-approach.md` + `04-checks/` | — |
| F-007 | 2026-08-24 | accessibility — 18px status chip vs WCAG 2.2 SC 2.5.8 | Design System lane | An 18px chip is a target-size risk: WCAG's own Understanding examples include "Menu items 18 CSS pixels high FAIL due to circle intersection". It conforms only via the Spacing exception, which needs a 24px circle centred on each chip not to intersect another target — so >=6px clear gap between adjacent chips in EVERY direction, including on wrap. Pairs with F-002. Evidence: https://www.w3.org/TR/WCAG22/#target-size-minimum | OPEN — awaiting Singh | — |
| F-008 | 2026-08-24 | component — Button/Split uses FIXED height | Design System lane | Split alone uses `height: 30px` where all 7 sibling types use `min-height`, so it cannot grow with its content. W3C C28: fixed-unit text containers risk cropping when text size is increased; C42 recommends the min- form. **Singh independently raised this** ("Giving the fix heigh can be problomatic for the action buttons"), so it already has owner support. Evidence: SPEC-07b §6. | OPEN — recommend prioritising | — |
| F-009 | 2026-08-24 | tokens — control sizes are px, not rem | Design System lane | Comparable systems (GitHub Primer) ship control sizes in rem so controls scale with root font size; ours are px. Because we use min-height, text can still push a control taller, so px is defensible — but it should be a decision, not a drift. Credible dissent both ways. | OPEN — awaiting Singh | — |
| F-010 | 2026-08-24 | tooling — raw-hex guard flags hex inside CSS COMMENTS | Fable, from the reference repo | The 8 reference .css files carry 11 raw hex values, ALL inside comments, documenting Figma's unbound/raw values as evidence (e.g. `table.css:382` records a RAW #0077FF confirmed unbound). That is valuable provenance. But `verify-build.py`'s no-raw-hex rule does not strip comments, so a feature that copies a block WITH its comments gets a false failure — and the likely "fix" is deleting the evidence. A hex inside `/* */` cannot affect rendering, so stripping comments before the check measures what the rule actually means rather than weakening it. | OPEN — fix is mine (verify-build.py), holding until the adopt agents release build files | — |
| F-011 | 2026-08-25 | governance — contradiction records decay silently | Fable + Opus (verification pass) | A record that says "the evidence contradicts this claim" is **true at the moment it is written and can become false without anything touching it**. Concrete case: `Wound Management/02-Conflicts/OPEN-QUERIES.json` Q3 carried `VERIFICATION_CONTRADICTS_THE_ANSWER` = "the export was never re-synced, still dated 2026-08-14". True when written 2026-08-24 16:35. **Singh re-exported at 19:40, which made it false — and it kept surfacing on every check run for ~19h until Opus's verification pass caught it.** The generalisation: **a contradiction record needs an expiry check, not just a creation.** Every `VERIFICATION_CONTRADICTS`-style field in this ecosystem has the same latent decay, including the CONTRADICTIONS register in `01-universal/` (C1-C10, S1-S11) — those rows assert "OPEN" states that may already have been resolved elsewhere. Proposed rule: any contradiction/conflict record carries a `last_reverified_at` and the check that reads it fails when that stamp is stale, exactly as `verify-open-queries.py` does for outstanding questions. | OPEN — proposed for the rules work | — |
| F-012 | 2026-08-25 | component — `Status` ships 5 hues; clinical `Healing` is blue and has no variant | Fable, from Opus's read of `26535:26277` + own token verification | **Successor to the retired F-003, and deliberately much narrower.** The `Status` component set exposes **green, red, yellow, neutral, purple**. Wound status has four clinical values and `Healing` is **blue** in Aman's artifact (`statusColor`), so three map cleanly (`Healed`→`active`/Success · `Not Healing`→`inactive`/Error · `Newly Reported`→`Pending`/Alert) and **`Healing` has no variant.** **What makes this NOT a colour invention — verified locally, and it is the reason this flag is not blocking:** `--tags-blue` **and** `--tags-blue-bg` are defined in **all five Agency colour modes** (`tokens.css` lines 250–251, 474–475, 698–699, 922, +1 — Light `#0077FF`/`#F0F7FF`), `build.py:498` already binds `.chip-blue`, and per Opus's variant map every `Status` hue binds the same generic pair (`tags/<hue>` for border+text, `tags/<hue>-bg` for fill). **So blue is a first-class, theme-aware hue in the token layer that the component set simply does not expose.** The gap is a **missing component variant, not a missing colour** — using `tags/blue` follows the component's own established binding pattern rather than departing from it. **Also verified, and it removes a second-order worry:** Opus read green's fill as `surface/success-subtle`, but that token and `tags/green-bg` are **byte-identical in all five modes** (`#F0FDF4`/`#052E16`/`#020617`/`#273A2D`/`#FFFFFF`), so binding the uniform `tags/*` pattern costs **zero** fidelity. | OPEN — needs Figma AI to add a blue variant, or Singh's word to use `tags/blue` under the existing pattern | **Recommendation (Singh's or Figma AI's call, not an AI's):** bind `tags/blue`/`tags/blue-bg` now under the component's own pattern and ask Figma AI to add the 6th variant, because the tokens exist and are theme-aware in all five modes. **Not actioned on AI judgement** — this component has already been revised four times. |
| F-013 | 2026-08-25 | governance — **an agent's own in-browser measurement silently outranks the spec that agent wrote** | Fable, from three same-day instances in one feature | **The most repeated defect on C360-45862, and no check catches it.** Three times in one feature I transcribed an authoritative Figma number into a spec, then overrode it with my own Chrome measurement, because measurement *felt* more evidence-backed than a number I had merely written down. **(1) Chip type** — `SPEC-08a` §148 marked it ✅ CONFIRMED (Inter Semi Bold 10 UPPERCASE) and §540–558 carried the finished CSS; I shipped 12/16/400 and labelled the spec's own value "unattested". **(2) Chip dot** — §6.1 records the dot as *hidden* in the table instance; I added an 8px dot. **(3) Column widths** — `SPEC-18` line 63 carries Singh's frame widths `115 · 123 · 150 · 237 · 87 · 150 · 206 · 60`, whose six scrollable values **sum to exactly the 953px scrollable region** and whose total `115+953+60` is **exactly the 1128px tab container** — arithmetic that self-validates the read. I then re-fitted every width by measuring rendered text, twice, shipping `LOCATION` at **170 where Singh's frame says 87** and `TYPE` at 200 where it says 237. **The aggravating factor: `verify-layout.mjs` explicitly EXCLUDES column widths**, on the reasoning that Aman's 11 columns are a different set from the frame's 7 so comparing them "would be a category error". That reasoning is half-right and produced the wrong action — it discarded Singh's numbers for the **seven columns present in both sets** instead of checking those and flagging only the four genuinely new ones. **A documented exclusion is where this class of defect hides**, because it reads as rigour. | OPEN — proposed as a universal rule + check | **Proposed rule:** where a spec states a Figma-sourced value, a build may not substitute a self-measured value without an override-ledger row naming the spec line it overrides. **Proposed check:** assert spec-stated geometry per named column/property and require an explicit `# OVERRIDES SPEC-nn line N` marker to skip — so the skip is a declaration, never a silence. **Cheap tell that would have caught #3 unaided:** Singh's widths sum to the region exactly; a fitted set that sums to 1490 into a 953 box cannot be the design. |
| F-014 | 2026-09-08 | kit — **17 radius declarations silently unset; 21 shorthand custom properties defined nowhere** | Claude (DS lane), verified across kit + repo + Wound Mgmt + poc + design-kit; severity corrected the same day by a fallback census | All 13 `02-components/*.css` files consumed 21 shorthands (`--fs-*`, `--lh-*`, `--fw-*`, `--ls-heading`, `--r-sm/--r-md`, `--rounded/-full/-none`, `--fonts-tracking-x-tracking-0/-neg-024`) with **0 definitions** anywhere — not in the kit (all folders), not in `caresmartz360-design-system/src/styles` (only `--rounded`), not in Wound Mgmt's generated `tokens.css`, not in poc or design-kit. **Severity split by fallback (this is the part that matters):** 46 of the 63 uses carried a `, <value>` fallback and rendered correctly — typography was never broken. **17 had NO fallback and were therefore invalid at computed-value time, and all 17 are RADIUS:** `--rounded` ×8, `--rounded-none` ×4, `--rounded-full` ×3, `--r-sm`, `--r-md`. Consequence: **every rounded corner the kit specifies rendered as a square** — buttons, tabs, table cells, column-panel rows, skeletons, the profile avatar (`--rounded-full` → a square avatar). Wound Mgmt's prototype consumed these 210 times and its 14/14 suite passed, so no check caught it. Very likely a real component-fidelity defect behind "components don't match the guidelines" (Singh, 2026-09-08). One shorthand mapped to a semantic that does not exist: `--lh-heading-5` (no `line-height/heading-5` in the export; H5 = 16/24 → `line-height/body-strong` per `page-header.json`). | **FIXED 2026-09-08** (Singh: "KIT-01 - Go") | Renamed all 21 to canonical export names across the 13 files — 71 lines, longest-first so `--rounded` could not corrupt `--rounded-full/-none`; `--fonts-tracking-x-tracking-0` (legacy X- name) collapsed to the literal `0`. **Validated:** 0 shorthands remain · line counts unchanged and every changed line identical after normalising variable names (0 structural anomalies) · all 136 vars the kit now consumes are defined by the export-derived token layer (was 21 undefined) · backup at `scratchpad/kit-backup/`. **Promoted check (needed — no existing check covers this):** every `var(--x)` consumed by `02-components/*.css` must be defined by the export-derived layer, and a `var()` without a fallback whose name is undefined is a hard failure. Assigned to Antigravity as a dry-run case. |
| F-015 | 2026-09-08 | kit — **no HTML markup examples exist for any kit component** | Claude (DS lane), `find … -iname '*.html'` over the whole kit | The kit ships CSS only; `00-README` promises copyable code but there are **0 `.html` files** anywhere under `Universal Html Rules/`, and no CSS header documents a skeleton. Faithful `.cs-*` markup exists **nowhere**: Wound Mgmt's `build.py` uses its own class vocabulary (`pnav-*`, `cell`, `tbl-frozen`, `act-group`) and emits only `cs-btn` and `cs-colpanel` from the kit. Codex's 2026-09-07 plan states "copyable HTML examples exist locally" — not true of this folder. Consequence: every consumer re-invents the DOM (the Wound Mgmt defect class F-013), and the 2026-09-08 reproduction had to author markup from selectors + contracts. | OPEN — proposed | **Fix:** each `02-components/<x>.css` gets a paired `<x>.html` skeleton (the minimal DOM the CSS assumes, generated once from the contract's `anatomy`), and the catalog previews render from those files — never from hand-written markup. `build_repro.py` (2026-09-08) validates that every `class="cs-…"` it emits exists as a selector in the kit — promote that as the check. |
